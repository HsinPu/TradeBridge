# 變更紀錄

## 1.1.1 — 2026-09-20

- 移除頂部「本機環境」與「已連線」標示，保留側欄版本與其他操作。

相容性：介面微調，由 1.1.0 升至 1.1.1；無 API 或資料庫變更。

驗證：Docker TypeScript／Vite 建置及容器健康檢查通過；37 項唯讀部署檢查通過，瀏覽器確認頂部已移除兩項標示，側欄版本仍顯示 v1.1.1。

## 1.1.0 — 2026-09-20

- 側邊欄底部顯示前端建置版本，收合時保留簡短版本號；版本由 Vite 讀取 package.json 注入，不在元件寫死。
- 「安全與環境」分別顯示前端建置版本及 runtime API 的後端運行版本，支援中英文標籤；讀取失敗顯示「無法取得」，不以其他版本替代。

相容性：向後相容的版本查詢介面功能，由 1.0.1 升至 1.1.0；無 API 契約及資料庫變更。

驗證：Docker 中 TypeScript 檢查與 Vite 建置通過，前後端容器健康；37 項唯讀部署檢查通過。瀏覽器確認側欄展開／收合的版本號與設定頁前後端版本均顯示 v1.1.0，並檢視實際版面。未重新執行完整後端測試，手機版及後端失敗狀態僅檢查程式邏輯。

## 1.0.1 — 2026-09-20

- 記錄持續有效的工作規則：每批修改完成、驗證及升版後，交付前主動 commit；push 與 tag 另依使用者指示執行。

相容性：文件規則更新，由 1.0.0 升至 1.0.1，產品行為與資料庫不變。

驗證：檢查文件格式及七處產品版本值一致；第三方鎖定套件資料不變。本批未改執行邏輯，不重跑完整測試。

## 1.0.0 — 2026-09-20

- 公開入口統一限制在 `APP_BASE_PATH` 下，預設 `/tradebridge/`。移除未加前綴的 API 代理、文件轉址及根目錄轉址，專案範圍外回傳 404。
- 修正 `/docs/project` 等路徑與舊規則衝突；支援 `/api`、`/redoc` 作為專案名稱。後端健康檢查帶上前綴，避免 `/api` 被重複解讀。
- 部署檢查新增 `--mode direct|proxy`，預設 direct。proxy 模式只請求專案內網址，並在送出前檢查正規化後的路徑；API Key 測試只使用公開前綴。
- 保留無尾斜線入口的查詢參數，更新外層代理範例、架構及專案指引；新增可重現的隔離 Docker 部署矩陣。

相容性：移除既有公開入口，依專案規則由 0.2.0 升至 1.0.0；目前尚未部署且無舊客戶端，不保留舊入口。此版本號表示公開介面不相容變更，不宣稱全部正式上線能力已完成。無資料庫結構變更。請重建前後端、改用帶前綴的網址；若 `.env` 覆寫 `APP_VERSION`，需同步更新或移除覆寫。後端內部路由仍為 `/api/v1`，連接埠不對外發布。

驗證：完整後端測試 195 項通過（含 25 項部署前綴及 7 項檢查工具測試）；Docker 中前端 TypeScript／Vite 建置通過。五種隔離部署 `/tradebridge`、`/tools/market-data`、`/docs/project`、`/api`、`/redoc` 全部通過，包含靜態資源、頁面 fallback、API 404、文件、查詢參數轉址、內部健康檢查與 API Key 的 POST/PATCH/DELETE。共用網域代理檢查 35 項通過，透過其他服務的存取紀錄確認檢查期間零跨專案請求。HTTPS 以代理協定標頭模擬，未配置正式憑證；後端測試仍有既有 Starlette/httpx 棄用提示。

## 0.2.0 — 2026-09-19

- 新增整站部署前綴，預設管理介面 `/tradebridge/`、API `/tradebridge/api/v1`、文件 `/tradebridge/docs`。
- 前端資源、API client、Nginx 與 FastAPI 共用 `APP_BASE_PATH`，支援經驗證的自訂路徑與尾端斜線正規化。
- 保留舊 `/api/v1` 的方法與請求內容；首頁與舊文件入口導向新的專案路徑。
- 保留可信任外層代理提供的公開網域與協定，讓文件、重新導向及環境 API 位址正確。
- 更新 Docker 健康檢查、部署文件與專案指引。

相容性：向後相容的新部署能力，因此由 0.1.0 升至 0.2.0。沒有資料庫結構變更。變更 `APP_BASE_PATH` 後須重新建置前端；`VITE_API_BASE_URL` 僅填來源網址，`API_PREFIX` 維持 `/api/v1`。既有 `.env` 若覆寫版本需同步調整。

驗證：完整後端測試 179 項通過（含新增部署前綴 16 項）；Docker 中 TypeScript 檢查及 Vite 建置通過；前後端容器健康檢查通過。實際部署唯讀檢查 16 項與隔離暫存資料庫的雙層代理檢查 24 項通過，含新舊 POST/PATCH、Key 驗證、公開 Host／HTTPS 協定資訊與尾端斜線正規化。瀏覽器已檢查四個主要頁面，設定頁 API 位址正確，Swagger 實際呼叫新路徑取得 200。HTTPS 驗證模擬代理協定標頭，未配置正式憑證或網域。

既有工具提示：後端測試有 Starlette/httpx 棄用提示；前端建置有大型 bundle 提示，npm 安裝回報 4 個依賴弱點（1 moderate、3 high），本次未變更第三方依賴。Windows 受限環境的本機 Vite 啟動受阻，已以 Docker 建置驗證。
